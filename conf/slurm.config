process {
    executor = 'slurm'
    errorStrategy = { task.exitStatus in [137, 140, 143] ? 'retry' : 'terminate' }
    maxRetries = 1
}

executor {
    // Keep the pilot gentle on a shared filesystem and scheduler.
    queueSize = 3
    submitRateLimit = '2 sec'
}

